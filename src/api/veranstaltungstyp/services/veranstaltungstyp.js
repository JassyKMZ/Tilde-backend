'use strict';

/**
 * veranstaltungstyp service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::veranstaltungstyp.veranstaltungstyp');
